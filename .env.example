# Registry Configuration
REGISTRY_URL=http://registry:5000
REGISTRY_AUTH_HTPASSWD_PATH=/auth/registry.password

# Web UI Configuration
WEB_UI_PORT=8080
SESSION_SECRET=change-this-to-a-random-secret-key

# Token Configuration
TOKEN_ISSUER=registry-token-issuer
TOKEN_EXPIRATION=900
TOKEN_KEY_PATH=/auth/token.key

# ACL Configuration
ACL_PATH=/auth/acl.json

# WebAuthn Configuration (for Passkeys)
# For production with HTTPS, update these values:
WEBAUTHN_RPID=localhost
WEBAUTHN_RPNAME=Aithen Registry
WEBAUTHN_ORIGIN=http://localhost:8080

# Production Example (uncomment and modify for your domain):
# WEBAUTHN_RPID=example.com
# WEBAUTHN_RPNAME=Aithen Registry
# WEBAUTHN_ORIGIN=https://example.com

# Optional: If registry uses different auth
# REGISTRY_USERNAME=admin
# REGISTRY_PASSWORD=admin
